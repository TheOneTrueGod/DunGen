import sys, os, pygamepygame.init()def sortFunc(file):	return len(file) * 1000000 + int(file[6:len(file) - 4])if len(sys.argv) == 2:	folder = sys.argv[1]	if len(sys.argv[1]) < 600 and os.path.exists(sys.argv[1]):		list = sorted([file for file in os.listdir(sys.argv[1]) if file[len(file) - 4:] == ".bmp" and file != "screen0.bmp"], key=sortFunc)		if list:			pic = pygame.image.load(os.path.join(folder, list[0]))			main_surface = pygame.Surface((pic.get_width() * len(list), pic.get_height()))			on = 0			while on < len(list):				main_surface.blit(pic, (on * pic.get_width(), 0))				on += 1				if on < len(list):					pic = pygame.image.load(os.path.join(folder, list[on]))			pygame.image.save(main_surface, os.path.join(folder, "../" + folder + ".PNG"))			del main_surfaceelse:	print "USAGE: python pictureCombiner.py <FolderName>"	print "Folder name should be the name of a folder that Explogen created."